import MagicString from 'magic-string';

declare const getTransformResult: (s: MagicString | undefined, id: string) => {
    code: string;
    map: any;
} | undefined;

export { getTransformResult };
